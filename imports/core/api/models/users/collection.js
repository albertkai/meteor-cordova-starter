/*
* Basic users collection defenition
* */

import { Meteor } from 'meteor/meteor';
import { SimpleSchema } from 'meteor/aldeed:simple-schema';

// const userSchema = new SimpleSchema({});

// Meteor.users.attachSchema(userSchema);
